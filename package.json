{
  "name": "fametypescript",
  "version": "1.0.0",
  "description": "Convert JSON of TypeScript Metamodel to an API",
  "main": "index.js",
  "scripts": {
    "update-from-json": "quicktype -t TypeScriptMM TypeScriptMM_fromPharo.json -o TypeScriptMMInterfaces_fromQuicktype.ts",
    "build": "ts-node src/codeGenerator.ts && npx mkdirp src/famix/model && mv target/famix/model/*.ts src/famix/model/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Christopher (Cris)",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.4.0",
    "mkdirp": "^1.0.4"
  },
  "dependencies": {
    "ts-morph": "^11.0.3"
  }
}
