// TODO: Java annontation types are @interface, whereas decorators in Java are functions
function FameProperty(container?:boolean, derived?:boolean, name:string = '*', opposite: string = '', type: /* Class<T> */ any = null) {
    // boolean container() default false;

    // boolean derived() default false;

    // String name() default "*";

    // String opposite() default "";

    // Class<?> type() default Void.class;

}